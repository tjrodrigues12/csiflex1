<Window x:Class="CSIFLEX.PartAnalyzer.Views.PartPerformanceGraph"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CSIFLEX.PartAnalyzer.Views"
        mc:Ignorable="d"
        Title="PartPerformanceGraph" Height="450" Width="800"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf" >
    <Grid>
        <Border Grid.Row="1"
                BorderBrush="#FFE2E1E1"
                BorderThickness="2"
                CornerRadius="2"
                Margin="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <TextBlock Text="{Binding Title}"/>
                <lvc:CartesianChart Grid.Row="2" Series="{Binding CycleTimeCollection}"  >
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis LabelsRotation="15" Labels="Cycle Off, Cycle On, Setup, Other" Position="LeftBottom" 
                                 >
                            <lvc:Axis.Separator >
                                <lvc:Separator Step="1"></lvc:Separator>
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Total Time" LabelFormatter="{Binding Formatter}" Position="RightTop"></lvc:Axis>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Grid>
        </Border>
    </Grid>
</Window>
